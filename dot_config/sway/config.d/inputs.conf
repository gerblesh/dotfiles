
input type:touchpad {
    natural_scroll enabled
    tap enabled
    accel_profile "flat"
    scroll_factor 0.3
}

input type:mouse {
    accel_profile "flat"
    pointer_accel 0.7
}

input type:keyboard {
    xkb_layout us,es
}
